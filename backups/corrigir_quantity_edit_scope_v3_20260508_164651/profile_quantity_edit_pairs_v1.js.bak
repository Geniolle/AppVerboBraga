// APPVERBO_PROFILE_QUANTITY_EDIT_PAIRS_V2_START
(function () {
  "use strict";

  //###################################################################################
  // (1) BOOTSTRAP E CONSTANTES
  //###################################################################################

  const bootstrap = window.__APPVERBO_BOOTSTRAP__ || {};
  const MEU_PERFIL_KEY_V2 = "meu_perfil";
  const LEGACY_DOCUMENTOS_KEY_V2 = "documentos";

  const sidebarMenuSettings = Array.isArray(bootstrap.sidebarMenuSettings)
    ? bootstrap.sidebarMenuSettings
    : [];

  const menuProcessQuantityValuesMap = (
    bootstrap.menuProcessQuantityValuesMap &&
    typeof bootstrap.menuProcessQuantityValuesMap === "object" &&
    !Array.isArray(bootstrap.menuProcessQuantityValuesMap)
  )
    ? bootstrap.menuProcessQuantityValuesMap
    : {};

  const renderStateByRuleKey = new Map();

  //###################################################################################
  // (2) NORMALIZAÇÃO
  //###################################################################################

  function normalizeKeyQuantityEditPairs_v2(value) {
    const cleanValue = String(value || "").trim().toLowerCase();

    if (cleanValue === LEGACY_DOCUMENTOS_KEY_V2) {
      return MEU_PERFIL_KEY_V2;
    }

    return cleanValue;
  }

  function normalizeTextQuantityEditPairs_v2(value) {
    return String(value || "")
      .normalize("NFD")
      .replace(/[\u0300-\u036f]/g, "")
      .trim()
      .toLowerCase()
      .replace(/\s+/g, " ");
  }

  function toSentenceCaseQuantityEditPairs_v2(value) {
    const cleanText = String(value || "").trim().replace(/\s+/g, " ");

    if (!cleanText) {
      return "";
    }

    const loweredText = cleanText.toLocaleLowerCase("pt-PT");
    return loweredText.charAt(0).toLocaleUpperCase("pt-PT") + loweredText.slice(1);
  }

  function normalizeFieldTypeQuantityEditPairs_v2(value) {
    const cleanType = String(value || "text").trim().toLowerCase();
    const allowedTypes = new Set(["text", "number", "email", "phone", "date", "flag", "list"]);

    return allowedTypes.has(cleanType) ? cleanType : "text";
  }

  function convertDisplayDateToInputDateQuantityEditPairs_v2(value) {
    const cleanValue = String(value || "").trim();

    if (/^\d{4}-\d{2}-\d{2}$/.test(cleanValue)) {
      return cleanValue;
    }

    if (/^\d{2}\/\d{2}\/\d{4}$/.test(cleanValue)) {
      const parts = cleanValue.split("/");
      return parts[2] + "-" + parts[1] + "-" + parts[0];
    }

    return cleanValue;
  }

  //###################################################################################
  // (3) CONFIGURAÇÃO DO MEU PERFIL
  //###################################################################################

  function getMeuPerfilSettingQuantityEditPairs_v2() {
    return sidebarMenuSettings.find(function (setting) {
      return normalizeKeyQuantityEditPairs_v2(setting && setting.key) === MEU_PERFIL_KEY_V2;
    }) || null;
  }

  function getFieldMetaMapQuantityEditPairs_v2(setting) {
    const metaMap = new Map();

    const options = Array.isArray(setting && setting.process_field_options)
      ? setting.process_field_options
      : [];

    options.forEach(function (option) {
      const fieldKey = normalizeKeyQuantityEditPairs_v2(option && option.key);

      if (!fieldKey) {
        return;
      }

      metaMap.set(fieldKey, {
        key: fieldKey,
        label: toSentenceCaseQuantityEditPairs_v2(option.label || fieldKey),
        fieldType: normalizeFieldTypeQuantityEditPairs_v2(option.field_type),
        isRequired: Boolean(option.is_required || option.required),
        listOptions: Array.isArray(option.list_options) ? option.list_options : []
      });
    });

    return metaMap;
  }

  function normalizeQuantityRulesQuantityEditPairs_v2(rawRules) {
    if (!Array.isArray(rawRules)) {
      return [];
    }

    return rawRules
      .map(function (rawRule, index) {
        if (!rawRule || typeof rawRule !== "object") {
          return null;
        }

        const key = normalizeKeyQuantityEditPairs_v2(
          rawRule.key ||
          rawRule.rule_key ||
          rawRule.ruleKey ||
          "qty_regra_" + String(index + 1)
        );

        const quantityFieldKey = normalizeKeyQuantityEditPairs_v2(
          rawRule.quantity_field_key ||
          rawRule.quantityFieldKey
        );

        const repeatedRaw = Array.isArray(rawRule.repeated_field_keys || rawRule.repeatedFieldKeys)
          ? (rawRule.repeated_field_keys || rawRule.repeatedFieldKeys)
          : [];

        const repeatedFieldKeys = [];
        const seen = new Set();

        repeatedRaw.forEach(function (rawFieldKey) {
          const fieldKey = normalizeKeyQuantityEditPairs_v2(rawFieldKey);

          if (!fieldKey || seen.has(fieldKey)) {
            return;
          }

          seen.add(fieldKey);
          repeatedFieldKeys.push(fieldKey);
        });

        const parsedMax = Number.parseInt(String(rawRule.max_items || rawRule.maxItems || "50"), 10);

        if (!key || !quantityFieldKey || !repeatedFieldKeys.length) {
          return null;
        }

        return {
          key: key,
          label: toSentenceCaseQuantityEditPairs_v2(rawRule.label || rawRule.rule_label || rawRule.name || "Quantidade"),
          quantityFieldKey: quantityFieldKey,
          repeatedFieldKeys: repeatedFieldKeys,
          headerKey: normalizeKeyQuantityEditPairs_v2(rawRule.header_key || rawRule.headerKey),
          itemLabel: toSentenceCaseQuantityEditPairs_v2(rawRule.item_label || rawRule.itemLabel || "Item") || "Item",
          maxItems: Number.isFinite(parsedMax) ? Math.max(1, Math.min(parsedMax, 50)) : 50
        };
      })
      .filter(Boolean);
  }

  function normalizeQuantityItemsQuantityEditPairs_v2(rawItems) {
    if (!Array.isArray(rawItems)) {
      return [];
    }

    return rawItems
      .map(function (rawItem) {
        if (!rawItem || typeof rawItem !== "object") {
          return null;
        }

        const normalized = {};

        Object.keys(rawItem).forEach(function (rawKey) {
          const fieldKey = normalizeKeyQuantityEditPairs_v2(rawKey);

          if (!fieldKey) {
            return;
          }

          normalized[fieldKey] = String(rawItem[rawKey] || "").trim();
        });

        return normalized;
      })
      .filter(Boolean);
  }

  function getQuantityValuesQuantityEditPairs_v2(ruleKey) {
    const valuesByMenu = menuProcessQuantityValuesMap[MEU_PERFIL_KEY_V2];

    if (!valuesByMenu || typeof valuesByMenu !== "object" || Array.isArray(valuesByMenu)) {
      return [];
    }

    return normalizeQuantityItemsQuantityEditPairs_v2(valuesByMenu[normalizeKeyQuantityEditPairs_v2(ruleKey)]);
  }

  //###################################################################################
  // (4) LOCALIZAR FORMULÁRIO
  //###################################################################################

  function getEditFormQuantityEditPairs_v2() {
    return (
      document.querySelector('form[action="/users/profile/personal"]') ||
      document.querySelector("#perfil-pessoal-card form.profile-edit-form") ||
      document.querySelector("#perfil-pessoal-card form")
    );
  }

  function getEditGridQuantityEditPairs_v2(form) {
    if (!form) {
      return null;
    }

    return form.querySelector(".personal-grid") ||
      form.querySelector(".form-grid") ||
      form;
  }

  function isEditFormVisibleQuantityEditPairs_v2(form) {
    if (!form) {
      return false;
    }

    const style = window.getComputedStyle(form);
    return style.display !== "none" && style.visibility !== "hidden";
  }

  function getFieldLabelQuantityEditPairs_v2(field) {
    const label = field ? field.querySelector("label, .personal-label") : null;
    return normalizeTextQuantityEditPairs_v2(label ? label.textContent : "");
  }

  function findFieldWrapperByFieldKeyQuantityEditPairs_v2(form, fieldKey, fieldMetaMap) {
    const cleanFieldKey = normalizeKeyQuantityEditPairs_v2(fieldKey);

    if (!form || !cleanFieldKey) {
      return null;
    }

    const directSelectors = [
      '[name="custom_field__' + cleanFieldKey + '"]',
      '[name="' + cleanFieldKey + '"]',
      '[data-profile-field-key="' + cleanFieldKey + '"]'
    ];

    for (const selector of directSelectors) {
      const input = form.querySelector(selector);

      if (input) {
        return input.closest(".field") || input.parentElement;
      }
    }

    const meta = fieldMetaMap.get(cleanFieldKey);
    const expectedLabel = normalizeTextQuantityEditPairs_v2(meta ? meta.label : cleanFieldKey);

    return Array.from(form.querySelectorAll(".field")).find(function (field) {
      if (field.closest("[data-appverbo-quantity-edit-generated-v2='1']")) {
        return false;
      }

      return getFieldLabelQuantityEditPairs_v2(field) === expectedLabel;
    }) || null;
  }

  function findInputByFieldKeyQuantityEditPairs_v2(form, fieldKey, fieldMetaMap) {
    const field = findFieldWrapperByFieldKeyQuantityEditPairs_v2(form, fieldKey, fieldMetaMap);

    if (!field) {
      return null;
    }

    return field.querySelector("input, select, textarea");
  }

  //###################################################################################
  // (5) CRIAR INPUTS DOS PARES
  //###################################################################################

  function createLabelQuantityEditPairs_v2(text, required) {
    const label = document.createElement("label");
    label.textContent = text || "-";

    if (required) {
      const mark = document.createElement("span");
      mark.textContent = " *";
      label.appendChild(mark);
    }

    return label;
  }

  function createInputQuantityEditPairs_v2(rule, fieldKey, fieldMeta, index, value) {
    const fieldType = normalizeFieldTypeQuantityEditPairs_v2(fieldMeta.fieldType);
    const name = "process_quantity_field__" + rule.key + "__" + String(index) + "__" + fieldKey;
    let input = null;

    if (fieldType === "list" && Array.isArray(fieldMeta.listOptions) && fieldMeta.listOptions.length) {
      input = document.createElement("select");

      fieldMeta.listOptions.forEach(function (optionValue) {
        const option = document.createElement("option");
        option.value = String(optionValue || "").trim();
        option.textContent = String(optionValue || "").trim();
        input.appendChild(option);
      });
    }
    else if (fieldType === "flag") {
      input = document.createElement("select");

      [
        ["", ""],
        ["1", "Sim"],
        ["0", "Não"]
      ].forEach(function (pair) {
        const option = document.createElement("option");
        option.value = pair[0];
        option.textContent = pair[1];
        input.appendChild(option);
      });
    }
    else {
      input = document.createElement("input");

      if (fieldType === "date") {
        input.type = "date";
      }
      else if (fieldType === "number") {
        input.type = "number";
      }
      else if (fieldType === "email") {
        input.type = "email";
      }
      else if (fieldType === "phone") {
        input.type = "tel";
      }
      else {
        input.type = "text";
      }
    }

    input.name = name;
    input.value = fieldType === "date"
      ? convertDisplayDateToInputDateQuantityEditPairs_v2(value)
      : String(value || "");

    input.dataset.appverboQuantityFieldKeyV2 = fieldKey;
    input.dataset.appverboQuantityRuleKeyV2 = rule.key;
    input.dataset.appverboQuantityIndexV2 = String(index);

    if (fieldMeta.isRequired) {
      input.required = true;
    }

    return input;
  }

  function createFieldQuantityEditPairs_v2(rule, fieldKey, fieldMeta, index, value) {
    const field = document.createElement("div");

    field.className = "field appverbo-quantity-edit-field-v2";
    field.setAttribute("data-appverbo-quantity-field-v2", "1");
    field.setAttribute("data-profile-field-key", "quantity_" + rule.key + "_" + String(index) + "_" + fieldKey);

    const label = createLabelQuantityEditPairs_v2(fieldMeta.label || fieldKey, fieldMeta.isRequired);
    const input = createInputQuantityEditPairs_v2(rule, fieldKey, fieldMeta, index, value);

    field.appendChild(label);
    field.appendChild(input);

    return field;
  }

  function createGroupQuantityEditPairs_v2(rule, index, itemValues, fieldMetaMap) {
    const group = document.createElement("div");

    group.className = "appverbo-quantity-edit-group-v2";
    group.setAttribute("data-appverbo-quantity-edit-generated-v2", "1");
    group.setAttribute("data-appverbo-quantity-rule-key-v2", rule.key);
    group.setAttribute("data-appverbo-quantity-index-v2", String(index));
    group.style.gridColumn = "1 / -1";
    group.style.order = String(5000 + index);

    const title = document.createElement("div");
    title.className = "appverbo-quantity-edit-title-v2";
    title.textContent = rule.itemLabel + " " + String(index + 1);
    group.appendChild(title);

    const innerGrid = document.createElement("div");
    innerGrid.className = "appverbo-quantity-edit-grid-v2";
    group.appendChild(innerGrid);

    rule.repeatedFieldKeys.forEach(function (fieldKey) {
      const fieldMeta = fieldMetaMap.get(fieldKey) || {
        key: fieldKey,
        label: toSentenceCaseQuantityEditPairs_v2(fieldKey),
        fieldType: "text",
        isRequired: false,
        listOptions: []
      };

      const fieldElement = createFieldQuantityEditPairs_v2(
        rule,
        fieldKey,
        fieldMeta,
        index,
        itemValues ? itemValues[fieldKey] : ""
      );

      innerGrid.appendChild(fieldElement);
    });

    return group;
  }

  //###################################################################################
  // (6) PAYLOAD OCULTO
  //###################################################################################

  function collectRulePayloadQuantityEditPairs_v2(form, rule) {
    const payload = [];
    const generatedInputs = Array.from(
      form.querySelectorAll(
        '[data-appverbo-quantity-rule-key-v2="' + rule.key + '"] input, ' +
        '[data-appverbo-quantity-rule-key-v2="' + rule.key + '"] select, ' +
        '[data-appverbo-quantity-rule-key-v2="' + rule.key + '"] textarea'
      )
    );

    generatedInputs.forEach(function (input) {
      const index = Number.parseInt(String(input.dataset.appverboQuantityIndexV2 || "0"), 10);
      const fieldKey = normalizeKeyQuantityEditPairs_v2(input.dataset.appverboQuantityFieldKeyV2 || "");

      if (!Number.isFinite(index) || !fieldKey) {
        return;
      }

      if (!payload[index]) {
        payload[index] = {};
      }

      payload[index][fieldKey] = String(input.value || "").trim();
    });

    return payload.filter(function (item) {
      return item && Object.keys(item).some(function (key) {
        return String(item[key] || "").trim();
      });
    });
  }

  function syncHiddenPayloadQuantityEditPairs_v2(form, rule) {
    if (!form || !rule) {
      return;
    }

    const payloadName = "process_quantity_payload__" + rule.key;

    Array.from(form.querySelectorAll('[name="' + payloadName + '"]')).forEach(function (input) {
      input.remove();
    });

    const hiddenInput = document.createElement("input");
    hiddenInput.type = "hidden";
    hiddenInput.name = payloadName;
    hiddenInput.value = JSON.stringify(collectRulePayloadQuantityEditPairs_v2(form, rule));

    form.appendChild(hiddenInput);
  }

  function bindPayloadSyncQuantityEditPairs_v2(form, rule) {
    const bindKey = "appverboQuantityPayloadBoundV2_" + rule.key;

    if (form.dataset[bindKey] === "1") {
      return;
    }

    form.dataset[bindKey] = "1";

    form.addEventListener("input", function (event) {
      const target = event.target;

      if (!target || !target.dataset || target.dataset.appverboQuantityRuleKeyV2 !== rule.key) {
        return;
      }

      syncHiddenPayloadQuantityEditPairs_v2(form, rule);
    }, true);

    form.addEventListener("change", function (event) {
      const target = event.target;

      if (!target || !target.dataset || target.dataset.appverboQuantityRuleKeyV2 !== rule.key) {
        return;
      }

      syncHiddenPayloadQuantityEditPairs_v2(form, rule);
    }, true);

    form.addEventListener("submit", function () {
      syncHiddenPayloadQuantityEditPairs_v2(form, rule);
    }, true);
  }

  //###################################################################################
  // (7) RENDER CONTROLADO SEM PISCAR
  //###################################################################################

  function removeGeneratedPairsQuantityEditPairs_v2(form, ruleKey) {
    if (!form) {
      return;
    }

    Array.from(
      form.querySelectorAll(
        '[data-appverbo-quantity-edit-generated-v1="1"][data-appverbo-quantity-rule-key-v1="' + ruleKey + '"], ' +
        '[data-appverbo-quantity-edit-generated-v2="1"][data-appverbo-quantity-rule-key-v2="' + ruleKey + '"]'
      )
    ).forEach(function (element) {
      element.remove();
    });
  }

  function getQuantityCountQuantityEditPairs_v2(quantityInput, rule) {
    const parsed = Number.parseInt(String(quantityInput ? quantityInput.value : "").trim(), 10);

    if (!Number.isFinite(parsed) || parsed <= 0) {
      return 0;
    }

    return Math.min(parsed, rule.maxItems || 50);
  }

  function buildRenderSignatureQuantityEditPairs_v2(rule, quantityCount, savedValues) {
    return JSON.stringify({
      ruleKey: rule.key,
      quantityCount: quantityCount,
      repeatedFieldKeys: rule.repeatedFieldKeys,
      savedValues: savedValues
    });
  }

  function renderRulePairsQuantityEditPairs_v2(form, grid, rule, fieldMetaMap, forceRender) {
    const quantityInput = findInputByFieldKeyQuantityEditPairs_v2(form, rule.quantityFieldKey, fieldMetaMap);
    const quantityWrapper = quantityInput ? quantityInput.closest(".field") : null;

    if (!quantityInput || !quantityWrapper || !grid) {
      return;
    }

    const quantityCount = getQuantityCountQuantityEditPairs_v2(quantityInput, rule);
    const savedValues = getQuantityValuesQuantityEditPairs_v2(rule.key);
    const signature = buildRenderSignatureQuantityEditPairs_v2(rule, quantityCount, savedValues);

    if (!forceRender && renderStateByRuleKey.get(rule.key) === signature) {
      syncHiddenPayloadQuantityEditPairs_v2(form, rule);
      return;
    }

    renderStateByRuleKey.set(rule.key, signature);
    removeGeneratedPairsQuantityEditPairs_v2(form, rule.key);

    let insertionPoint = quantityWrapper;

    for (let index = 0; index < quantityCount; index += 1) {
      const group = createGroupQuantityEditPairs_v2(
        rule,
        index,
        savedValues[index] || {},
        fieldMetaMap
      );

      insertionPoint.insertAdjacentElement("afterend", group);
      insertionPoint = group;
    }

    syncHiddenPayloadQuantityEditPairs_v2(form, rule);
    bindPayloadSyncQuantityEditPairs_v2(form, rule);
  }

  function renderAllPairsQuantityEditPairs_v2(forceRender) {
    const form = getEditFormQuantityEditPairs_v2();
    const grid = getEditGridQuantityEditPairs_v2(form);
    const setting = getMeuPerfilSettingQuantityEditPairs_v2();

    if (!form || !grid || !setting || !isEditFormVisibleQuantityEditPairs_v2(form)) {
      return;
    }

    const fieldMetaMap = getFieldMetaMapQuantityEditPairs_v2(setting);
    const rules = normalizeQuantityRulesQuantityEditPairs_v2(setting.process_quantity_fields);

    rules.forEach(function (rule) {
      renderRulePairsQuantityEditPairs_v2(form, grid, rule, fieldMetaMap, Boolean(forceRender));
    });
  }

  //###################################################################################
  // (8) BIND DO CAMPO QUANTIDADE
  //###################################################################################

  function bindQuantityInputsQuantityEditPairs_v2() {
    const form = getEditFormQuantityEditPairs_v2();
    const setting = getMeuPerfilSettingQuantityEditPairs_v2();

    if (!form || !setting || form.dataset.appverboQuantityEditPairsBoundV2 === "1") {
      return;
    }

    const fieldMetaMap = getFieldMetaMapQuantityEditPairs_v2(setting);
    const rules = normalizeQuantityRulesQuantityEditPairs_v2(setting.process_quantity_fields);

    rules.forEach(function (rule) {
      const quantityInput = findInputByFieldKeyQuantityEditPairs_v2(form, rule.quantityFieldKey, fieldMetaMap);

      if (!quantityInput) {
        return;
      }

      quantityInput.addEventListener("input", function () {
        renderStateByRuleKey.delete(rule.key);
        window.setTimeout(function () {
          renderAllPairsQuantityEditPairs_v2(true);
        }, 0);
      });

      quantityInput.addEventListener("change", function () {
        renderStateByRuleKey.delete(rule.key);
        window.setTimeout(function () {
          renderAllPairsQuantityEditPairs_v2(true);
        }, 0);
      });
    });

    form.dataset.appverboQuantityEditPairsBoundV2 = "1";
  }

  //###################################################################################
  // (9) ESTILO
  //###################################################################################

  function installStyleQuantityEditPairs_v2() {
    if (document.getElementById("appverbo-profile-quantity-edit-pairs-style-v2")) {
      return;
    }

    const style = document.createElement("style");
    style.id = "appverbo-profile-quantity-edit-pairs-style-v2";
    style.textContent = [
      ".appverbo-quantity-edit-group-v2 { width: 100%; margin-top: 14px; }",
      ".appverbo-quantity-edit-title-v2 { margin: 0 0 10px; font-size: 14px; font-weight: 800; color: #0f172a; }",
      ".appverbo-quantity-edit-grid-v2 { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 12px 16px; }",
      ".appverbo-quantity-edit-grid-v2 .field { min-height: 54px; }",
      ".appverbo-quantity-edit-grid-v2 .field input, .appverbo-quantity-edit-grid-v2 .field select, .appverbo-quantity-edit-grid-v2 .field textarea { width: 100%; }",
      "@media (max-width: 900px) { .appverbo-quantity-edit-grid-v2 { grid-template-columns: 1fr; } }"
    ].join("\n");

    document.head.appendChild(style);
  }

  //###################################################################################
  // (10) INICIAR
  //###################################################################################

  function startQuantityEditPairs_v2() {
    installStyleQuantityEditPairs_v2();
    bindQuantityInputsQuantityEditPairs_v2();
    renderAllPairsQuantityEditPairs_v2(false);
  }

  document.addEventListener("click", function (event) {
    const button = event.target && event.target.closest
      ? event.target.closest('[data-edit-target="perfil-pessoal-card"], .profile-edit-toggle')
      : null;

    if (!button) {
      return;
    }

    renderStateByRuleKey.clear();

    window.setTimeout(startQuantityEditPairs_v2, 80);
    window.setTimeout(startQuantityEditPairs_v2, 250);
  }, true);

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", startQuantityEditPairs_v2);
  }
  else {
    startQuantityEditPairs_v2();
  }

  window.addEventListener("pageshow", startQuantityEditPairs_v2);
})();
// APPVERBO_PROFILE_QUANTITY_EDIT_PAIRS_V2_END
